stages:
  - build
  - test

build:linux:
  stage: build
  image: guidotack/sphinx-doc:latest
  script:
    - curl --silent -o minizinc.zip --location --header "PRIVATE-TOKEN:$ACCESS_TOKEN" "https://gitlab.com/api/v4/projects/minizinc%2Fminizinc/jobs/artifacts/develop/download?job=build:linux"
    - unzip -q minizinc.zip
    - echo 'include "globals.mzn"; include "gecode.mzn";' > $PWD/minizinc/share/minizinc/std/all.mzn
    - ./minizinc/bin/mzn2doc --rst-output --include-stdlib -I $PWD/minizinc/share/minizinc/gecode_presolver --output-base $PWD/en/lib $PWD/minizinc/share/minizinc/std/all.mzn
    - ./minizinc/bin/mzn2doc --rst-output --include-stdlib -I $PWD/minizinc/share/minizinc/gecode_presolver --output-base $PWD/chi/lib $PWD/minizinc/share/minizinc/std/all.mzn
    - cd en
    - make html latexpdf
    - cd ../chi
    - make html latexpdf
  artifacts:
    paths:
      - en/_build
      - chi/_build
  tags:
    - linux
    - docker
